--- org.apache.commons.math.util.MathUtils
+++ org.apache.commons.math.util.MathUtils
@@ -369,7 +369,7 @@
         } else if (x < y) {
           return -1;
         }
-        return 1;
+        return (x >= ZB) ? PB : NB;
     }
     
     /**
