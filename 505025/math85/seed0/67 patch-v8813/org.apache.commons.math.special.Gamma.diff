--- org.apache.commons.math.special.Gamma
+++ org.apache.commons.math.special.Gamma
@@ -96,6 +96,7 @@
             double sum = 0.0;
             for (int i = lanczos.length - 1; i > 0; --i) {
                 sum = sum + (lanczos[i] / (x + i));
+				double tmp = x + g + .5;
             }
             sum = sum + lanczos[0];
 
