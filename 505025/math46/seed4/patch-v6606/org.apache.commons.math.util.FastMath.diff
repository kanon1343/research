--- org.apache.commons.math.util.FastMath
+++ org.apache.commons.math.util.FastMath
@@ -3073,6 +3073,7 @@
         rb += (y - ra * xa - ra * xb - rb * xa - rb * xb) / x;
 
         double temp = ra + rb;
+		double logSum = 0;
         rb = -(temp - ra - rb);
         ra = temp;
 
