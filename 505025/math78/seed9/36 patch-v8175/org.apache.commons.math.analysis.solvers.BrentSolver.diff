--- org.apache.commons.math.analysis.solvers.BrentSolver
+++ org.apache.commons.math.analysis.solvers.BrentSolver
@@ -283,6 +283,7 @@
                     oldDelta = delta;
                 } else {
                     oldDelta = delta;
+					this.f = f;
                     delta = p / p1;
                 }
             }
