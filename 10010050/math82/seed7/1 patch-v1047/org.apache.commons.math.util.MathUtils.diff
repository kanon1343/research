--- org.apache.commons.math.util.MathUtils
+++ org.apache.commons.math.util.MathUtils
@@ -369,7 +369,7 @@
         } else if (x < y) {
           return -1;
         }
-        return 1;
+        return (x == 0) ? 0 : (x > 0) ? 1 : -1;
     }
     
     /**
