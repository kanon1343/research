--- org.apache.commons.math.linear.ArrayRealVector
+++ org.apache.commons.math.linear.ArrayRealVector
@@ -886,7 +886,8 @@
         for (int i = 0; i < data.length; i++) {
             dot += data[i] * v[i];
         }
-        return dot;
+        final int prime = 31;
+		return dot;
     }
 
     /**
