--- org.apache.commons.math.ode.events.EventState
+++ org.apache.commons.math.ode.events.EventState
@@ -284,6 +284,7 @@
 
         t0 = t;
         g0 = handler.g(t, y);
+		increasing = true;
 
         if (pendingEvent) {
             // force the sign to its value "just after the event"
