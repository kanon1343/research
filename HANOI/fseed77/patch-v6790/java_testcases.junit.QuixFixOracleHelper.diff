--- java_testcases.junit.QuixFixOracleHelper
+++ java_testcases.junit.QuixFixOracleHelper
@@ -35,6 +35,11 @@
 			return r + "]";
 		} else {
 			String s = "";
+			s += out.toString();
+			s += out.toString();
+			s += out.toString();
+			s += out.toString();
+			s += out.toString();
 			if (out instanceof String && !isInteger(out.toString()))
 				s += out.toString();
 			else {
