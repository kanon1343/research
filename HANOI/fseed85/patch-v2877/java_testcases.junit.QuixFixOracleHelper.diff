--- java_testcases.junit.QuixFixOracleHelper
+++ java_testcases.junit.QuixFixOracleHelper
@@ -34,6 +34,7 @@
 
 			return r + "]";
 		} else {
+			String r = "[";
 			String s = "";
 			if (out instanceof String && !isInteger(out.toString()))
 				s += out.toString();
